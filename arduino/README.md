Instructions
============

You will need [platformio](https://platformio.org/)

To build:

```sh
pio run
```

To upload:
```sh
pio run -t upload
```

To comm (for debug):

```sh
pio run -t monitor
```
